# 2026-VK-EDU-Web-11-Petrukhina-A

Учебный Django-проект сайта вопросов и ответов **«Поясняем на котятах»**.

## Страницы

- `/` — новые вопросы
- `/hot/` — популярные вопросы
- `/tag/cats/` — вопросы по тегу
- `/question/1/` — страница вопроса
- `/ask/` — создание вопроса
- `/login/` — вход
- `/signup/` — регистрация
- `/profile/` — профиль

## Запуск локально

```bash

Создать `.env.local` по примеру `.env.example`.
python -m venv .venv
.venv\Scripts\Activate.ps1
pip install -r requirements.txt
python manage.py migrate
python manage.py fill_db 10

Запустить сервисы:
docker start redis-local
docker start maildev-local

Если контейнеров ещё нет:
docker run --name redis-local -p 6379:6379 -d redis:7
docker run --name maildev-local -p 1080:1080 -p 1025:1025 -d maildev/maildev

В отдельных терминалах запустить:
celery -A application worker -l info -P solo
celery -A application beat -l info
python manage.py runserver

Сайт: http://127.0.0.1:8000/
Maildev: http://127.0.0.1:1080/


```


## Что сделано
 ### ДЗ2
- Django-приложения `questions` и `core`
- маршруты в `urls.py`
- шаблоны с общим `base.html`
- повторяющиеся блоки через `include`
- пагинация через Django `Paginator`
- статика подключена через `{% static %}`
- ссылки сделаны через `{% url %}`
- добавлены `requirements.txt`, `.gitignore`, `Dockerfile`, `docker-compose.yml`

### ДЗ3
- добавлены модели `Profile`, `Tag`, `Question`, `Answer`, `QuestionLike`, `AnswerLike`;
- настроены связи между моделями;
- добавлены ограничения на повторные лайки;
- настроены Model Manager для новых, популярных вопросов и вопросов по тегу;
- настроена админка Django;
- добавлены inline-блоки профиля пользователя и ответов к вопросу;
- добавлена команда `fill_db`;
- страницы вопросов переведены на данные из БД;
- добавлены пустые состояния и обработка 404;
- подключён `django-debug-toolbar`;
- настройки вынесены в `.env`;
- обновлён `docker-compose.yml` с PostgreSQL.

### ДЗ4

- добавлены формы `login`, `signup`, `profile`;
- реализованы вход, регистрация, выход и редактирование профиля;
- обработан параметр `next` при входе;
- при регистрации создаётся связанный `Profile`;
- пароль проверяется стандартными валидаторами Django;
- добавлены формы создания вопроса и ответа через `ModelForm`;
- формы отправляются методом `POST` и используют CSRF;
- ошибки форм выводятся пользователю, введённые данные сохраняются;
- после создания вопроса пользователь переходит на страницу созданного вопроса;
- после добавления ответа пользователь возвращается к добавленному ответу;
- для неавторизованного пользователя недоступные действия заблокированы или ведут на страницу входа.

### ДЗ5

- добавлена загрузка аватарки через форму профиля;
- аватарки сохраняются в `MEDIA_ROOT`, а не в `STATIC_ROOT`;
- настроена отдача файлов из `MEDIA_URL` в режиме разработки;
- добавлена генерация непредсказуемых имён файлов для аватарок;
- добавлена проверка расширения и размера файла аватарки;
- реализованы AJAX-лайки и дизлайки вопросов;
- реализованы AJAX-лайки и дизлайки ответов;
- реализована AJAX-отметка правильного ответа;
- добавлена проверка авторизации для AJAX-действий;
- сервер возвращает JSON-ответы для AJAX-запросов;
- на клиенте обрабатываются ошибки и обновляется рейтинг без перезагрузки страницы.

### ДЗ6

Сделано:

- настроены Redis, Celery и Celerybeat;
- Redis используется как broker Celery и cache backend Django;
- для cache, broker и celerybeat используются разные Redis DB;
- настройки Redis и email вынесены в `.env`;
- добавлены сервисы `redis`, `celery`, `celerybeat`, `maildev` в `docker-compose.yml`;
- добавлены celery-задачи для обновления кеша популярных тегов и лучших пользователей;
- правая колонка получает данные из кеша с fallback в БД;
- настроена отправка email-уведомлений автору вопроса при новом ответе через celery-задачу.
- добавлен полнотекстовый поиск по заголовку и тексту вопроса;

Не реализовано:

- realtime через Centrifugo;
